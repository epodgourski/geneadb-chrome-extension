# Changelog

Все значимые изменения проекта фиксируются в этом файле.

Формат версии: **YYYY.MM.DD.NN**

---

## [2026.06.18.01] — 2026-06-18

### Добавлено
- **Санитизация имён файлов**: функция `sanitizeFilename()` в `sources/index.js`
  автоматически удаляет из имени файла символы, недопустимые в файловых системах
  Windows/Linux/macOS: `\ / : * ? " < > | ,`. Применяется централизованно в
  `parseUrl()` ко всем источникам.

---

## [2026.06.13.01] — 2026-06-13

### Изменено
- **Яндекс Архивы: полностью переработан механизм скачивания.**
  Старый подход (парсинг HTML → `thumb.path` → `image?id=...`) заменён на новый:
  1. Метаданные (`currentNode`, `filename`) читаются из `<script id="__NEXT_DATA__">`;
     при SPA-навигации (без перезагрузки) данные обновляются через запрос к
     Next.js data API (`/_next/data/{buildId}/.../pageNum.json`).
  2. URL оригинала ищется в `performance.getEntriesByType('resource')` по паттерну
     `/archive/api/image?id={currentId}&type=original`.
  3. Если оригинал ещё не загружен (пользователь не зумил), расширение
     автоматически кликает zoom+ и ожидает появления URL (до 15 сек).
  4. Скачивание: `fetch` с `credentials: 'include'` выполняется **в контексте
     страницы** (`world: 'MAIN'`), что обеспечивает передачу cookies ya.ru.
     Результат (blob → dataURL) передаётся в `chrome.downloads.download`.
  5. Имя файла берётся из `currentNode.namepath` (например `609-2-95-00000038.jpg`)
     вместо прежнего `f0001.jpeg`.

### Добавлено
- Утилита `runInPage(tabId, func, args, world)` — обёртка над
  `chrome.scripting.executeScript` с поддержкой параметра `world` для
  выполнения скриптов в основном контексте страницы (`MAIN`).
- Функция `downloadDirect` для скачивания через fetch в контексте страницы
  (обход ограничения: service worker не имеет cookies ya.ru).
- Функция `debugLog` и блок `#debug-log` в popup для отладки — показывает
  пошаговый статус сканирования и скачивания.
- Обработчик `downloadDirect` в `background.js` (запасной вариант).

---

## [2026.06.06.07] — 2026-06-06

### Добавлено
- **Устойчивость длинных серий (300+ кадров)** к выгрузке service worker:
  - прогресс серии (список ARK, номер текущего кадра, total) сохраняется в
    `chrome.storage.session`;
  - keep-alive через `chrome.alarms` (период 0.5 мин) удерживает/будит service worker;
  - если Chrome всё же выгрузит SW посреди серии (на старых версиях есть жёсткий
    лимит времени жизни), при перезапуске/срабатывании будильника скачивание
    **автоматически продолжается с последнего сохранённого кадра**;
  - фаза скачивания не зависит от вкладки — файлы тянутся напрямую с familysearch,
    поэтому серия не прервётся, даже если вкладку закрыть после сбора кадров.
- Права `storage` и `alarms` в `manifest.json`.

---

## [2026.06.06.06] — 2026-06-06

### Изменено
- **Скачивание серии перенесено в service worker** (`background.js`). Раньше цикл
  крутился в попапе и **прерывался при закрытии/потере фокуса попапом** (Chrome
  выгружает popup). Теперь попап лишь запускает процесс и показывает прогресс,
  а навигация по кадрам, сбор ARK и скачивание выполняются в фоне и **продолжаются
  даже после закрытия попапа**.
  - Протокол: `startSeries` / `cancelSeries` / `getSeriesState` + сообщения
    `seriesProgress` для обновления прогресса в попапе.
  - При повторном открытии попапа во время активного скачивания прогресс
    восстанавливается из состояния фонового процесса.
  - Отмена — повторным нажатием кнопки (как и раньше).

---

## [2026.06.06.05] — 2026-06-06

### Добавлено
- **PNG-иконки расширения** (`icon16/32/48/128.png`), сгенерированные из `vrodu.svg`,
  прописаны в `manifest.json` (`icons` + `action.default_icon`) — иконка теперь
  корректно отображается на панели инструментов Chrome (SVG там не поддерживается).

### Изменено
- **Тема по умолчанию** — «🏛️ Архивный коричневый» (`theme-1`) вместо «🌿 Винтаж зелень».

---

## [2026.06.06.04] — 2026-06-06

### Изменено
- **Новая иконка расширения** — логотип ВРДУ (`vrodu.svg`) вместо прежней `icon128.svg`:
  - используется как иконка расширения и кнопки на панели (`icons` и `action.default_icon` в `manifest.json`);
  - заменяет эмодзи 📄 в шапке попапа.
- Удалён неиспользуемый файл `icon128.svg`.

---

## [2026.06.06.03] — 2026-06-06

### Исправлено
- **ARK всегда берётся в формате `3:1:XXXX`** (имя файла — часть после `3:1:`,
  например `3QHK-V7TF-73W2`). На сериях, где `id` ячейки служебный
  (`grid-item-TH-...`), нужный `3:1:`-ARK теперь извлекается из `src` миниатюры,
  а не из `id`. Источники ARK по приоритету: `id` ячейки → `src` миниатюры →
  вложенный `id`. Если `3:1:`-ARK не найден, кадр пропускается (без появления
  `TH-...` в имени файла).
- **Главный детектор серии** — `input[type="number"][min="1"]` с `max > 1`:
  единственный признак, надёжно срабатывающий во всех проверенных случаях
  (`groupId` присутствует не всегда, число отрендеренных кадров сетки не равно
  общему из-за виртуального скролла).

---

## [2026.06.06.01] — 2026-06-06

### Добавлено
- **Пакетное скачивание серии снимков** с FamilySearch
  - Кнопка **«Скачать серию (N)»** в UI, появляется только на FamilySearch при наличии серии (N — общее число снимков)
  - **Языконезависимая детекция** серии (не зависит от локализации интерфейса):
    - числовое поле `input[type="number"][min="1"]` с `max > 1` — основной признак серии и источник общего числа кадров;
    - параметр `groupId` в URL и наличие `[role="option"][image-index]` — вспомогательные признаки.
  - **Сбор ARK всех кадров** по техническим атрибутам, без привязки к тексту:
    - чтение сетки `[role="option"][image-index]` с прокруткой контейнера (обход виртуального скролла), `image-index` задаёт порядок кадра;
    - ARK берётся из `id` (`grid-item-<ARK>`), поддерживаются оба формата идентификаторов;
    - фолбэк-навигация кнопкой «вперёд», которая ищется по позиции относительно поля ввода (а не по `aria-label`), с чтением ARK текущего кадра.
  - К существующему имени файла добавляется числовой префикс `NN_` (ширина с ведущими нулями: 2 знака до 99 снимков, 3 знака от 100)
  - Прогресс в тексте кнопки («Скачивается 3 / 40...») и отмена повторным нажатием
  - Пауза 400 мс между скачиваниями, чтобы не перегружать сервер
- Чтение DOM активной вкладки через `chrome.scripting.executeScript` (используются уже имеющиеся разрешения `scripting` + `activeTab`)

### Изменено
- Логика загрузки вынесена в переиспользуемую функцию `requestDownload`; серия использует тот же Service Worker с `credentials: 'include'`, что и одиночное скачивание FamilySearch

---

## [2026.03.28.01] — 2026-03-28

### Добавлено
- Новый источник **Яндекс Архивы** (`ya.ru/archive/catalog/{каталог}/{N}`)
  - Публичный доступ, авторизация не требуется
  - Получение ID изображения через fetch текущего URL и поиск `"thumb":{"path":"..."}` в ответе
  - Скачивание оригинала через `ya.ru/archive/api/image?id=...&type=original`
  - Имя файла: `fNNNN.jpeg` (номер страницы, дополненный нулями до 4 знаков с префиксом `f`)
- `host_permissions` для `ya.ru` в `manifest.json`

### Изменено
- Архитектура расширения: добавлена поддержка `needsPageScan` — опциональный шаг `scanPage(url)` перед парсингом для источников, требующих получения данных со страницы
- `parseUrl` и `processCurrentTab` принимают параметр `extra` с данными, полученными при сканировании
- `processCurrentTab` стал асинхронным (`async`)
- Убрано дублирование `detectSource` в обработчике `DOMContentLoaded`
- Исправлена неиспользуемая переменная `response` в `downloadImage`

---

## [2026.03.21.02] — 2026-03-21

### Добавлено
- Источник **FamilySearch** (`familysearch.org`)
  - Аутентификация через cookies браузера
  - Парсинг кода между `:` и `?` в URL
  - Загрузка через `sg30p0.familysearch.org`
- Источник **RusNEB** (`viewer.rusneb.ru`)
  - Публичный доступ
  - Парсинг кода документа и номера страницы
  - Имя файла: `КОД_NNNN.jpeg`
- Service Worker (`background.js`) для загрузки файлов через `chrome.downloads`
- 6 цветовых тем с сохранением выбора в `localStorage`
- Модульная архитектура источников (`sources` + `detectSource` + `parseUrl`)
